{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
       
       <h1 class="title p-1 text-center">{{article.title}}</h1>
      <div class="img-fluid">
          <img width = "100%" src="/assets/blog/images/{{article.image}}" alt ="{{article.title}}" />
      </div>
      <div class="content">
          <p class="date">Ecrit le {{article.createdAt | date("m/d/Y")}} à {{article.createdAt | date("H:i")}} par 
            <span class="author"> {{article.author}}</span></p>
          <h2 class="title p-1">{{article.title}}</h2>
          <p class="content text-justify">{{article.content}} </p>
     </div>
     
{% endblock %}
